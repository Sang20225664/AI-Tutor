apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: ai-tutor-prod
data:
  NODE_ENV: "production"
  MODE: "prod"
  PORT: "5000"
  LOG_LEVEL: "warn"  # Prod: Minimal logging
  API_BASE: "/api"
  CORS_ORIGIN: "https://ai-tutor.example.com"  # Prod: Strict CORS (change domain!)
