apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: ai-tutor-prod
type: Opaque
stringData:
  # PRODUCTION: MUST CHANGE THESE VALUES BEFORE DEPLOYMENT!
  GEMINI_API_KEY: "CHANGE-ME-PRODUCTION-GEMINI-API-KEY"
  JWT_SECRET: "CHANGE-ME-PRODUCTION-JWT-SECRET-STRONG-32CHARS"
  MONGODB_URI: "mongodb://mongodb.ai-tutor-prod.svc.cluster.local:27017/ai_tutor"
