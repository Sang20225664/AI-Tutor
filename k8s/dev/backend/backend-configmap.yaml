apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: ai-tutor-dev
data:
  NODE_ENV: "development"
  MODE: "dev"
  PORT: "5000"
  LOG_LEVEL: "debug"  # Dev: Verbose logging
  API_BASE: "/api"
  CORS_ORIGIN: "*"  # Dev: Allow all origins
