apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvc-dev
  namespace: ai-tutor-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi  # Dev: Smaller storage
