apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvc-staging
  namespace: ai-tutor-staging
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi  # Staging: Medium storage
